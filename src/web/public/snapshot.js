/*
 * Placeholder. The real file is written by `npm run export:static`, which
 * overwrites this one in the output folder with the exported story data.
 *
 * Kept here so the shell builds and serves without any project baked into it —
 * a shell with no snapshot shows the "couldn't load" screen, which is the
 * correct behaviour for an incomplete upload.
 */
window.__ZN_SNAPSHOT__ = null
