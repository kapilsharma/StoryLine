---
uid: n_d0c3a79a
title: 'Changing id, or a one-sided spouse'
boards:
  - files
tags:
  - files
  - characters
created: '2026-08-17'
---
`id` must match the filename stem, and it is what `board.json` and the family fields point at. Rename with the app rather than the filesystem.

`spouse` is symmetric — the app writes both sides. Adding it to one file only shows up as an *asymmetric spouse* problem on the family tree.
