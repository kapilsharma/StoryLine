---
uid: n_f7edc79a
title: 'npm run export:static -- --project <dir> --out <dir>'
boards:
  - reference
tags:
  - publishing
created: '2026-08-17'
---
```bash
npm run export:static -- --project ~/novel --out dist-site
```

Open `dist-site/index.html` locally to check it, then upload the folder's **contents** to your host. Re-exporting into the same folder is always safe.
